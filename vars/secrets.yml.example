---
# Example secure variables file
# 
# IMPORTANT: This file should be encrypted with Ansible Vault or stored securely
# DO NOT commit this file with real credentials to version control
#
# To encrypt this file:
#   ansible-vault encrypt vars/secrets.yml
#
# To use the encrypted file:
#   ansible-playbook -i inventory.yml deploy.yml --ask-vault-pass
#   # or
#   ansible-playbook -i inventory.yml deploy.yml --vault-password-file ~/.vault_pass

# VPN credentials (replace with your actual credentials)
gluetun:
  openvpn_user: "your_vpn_username"
  openvpn_password: "your_vpn_password"
