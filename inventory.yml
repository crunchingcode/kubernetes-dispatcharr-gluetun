---
# Ansible inventory for deploying to Kubernetes
# This deployment runs on localhost and uses your local kubeconfig

all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
